---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import FAQ from '../../components/FAQ.astro';
import Disclosure from '../../components/Disclosure.astro';
import HowWeResearch from '../../components/HowWeResearch.astro';
import { articleSchema, itemListSchema, faqSchema, breadcrumbSchema } from '../../lib/schema';

const title = 'Best Chainsaw for Firewood UK 2026 - Processing Logs Efficiently';
const description = 'Independent, research-led guide to the best chainsaws for processing firewood in the UK: bar length, power type and what actually speeds up a log pile.';
const datePublished = '2026-07-10';
const url = '/chainsaw-for-firewood/';

const crumbs = [
  { name: 'Home', url: '/' },
  { name: 'Chainsaws for firewood', url },
];

// TODO(content agent): replace with real, sourced FAQs.
const faqs: { q: string; a: string }[] = [];
// TODO(content agent): populate with real ranked PickCard picks once sourced; itemListSchema activates automatically.
const picks: { name: string; url: string }[] = [];

const schema = [
  articleSchema({ headline: title, description, url, datePublished, author: 'Sunny Patel' }),
  ...(picks.length ? [itemListSchema({ name: 'Best chainsaws for firewood UK', url, items: picks.map((p, i) => ({ ...p, position: i + 1 })) })] : []),
  faqSchema(faqs),
  breadcrumbSchema(crumbs),
].filter(Boolean);
---
<BaseLayout {title} {description} {schema}>
  <article class="max-w-4xl mx-auto px-4 py-10">
    <Breadcrumbs items={crumbs} />
    <p class="text-sm font-semibold text-hazard-600 uppercase tracking-wide mb-2 font-heading">Firewood</p>
    <h1 class="text-3xl md:text-5xl font-bold text-graphite-900 leading-tight font-heading">Best chainsaw for processing firewood</h1>
    <p class="mt-5 text-lg text-steel-600 leading-relaxed">
      Firewood processing is repetitive, sustained cutting through rounds, usually 15 to 25cm
      across. This guide covers the power type and bar length that actually speeds up a log pile
      without over-buying.
    </p>
    <Disclosure />

    <!-- TODO(content agent): full buying-guide prose, PickCard picks, PriceGrid comparisons, comparison table, sourced FAQs. -->
    <div class="my-12 rounded-xl border border-dashed border-steel-400 bg-surface-100 p-6 text-sm text-steel-600">
      Placeholder shell. Full guide content pending from the wave-1 content pipeline.
    </div>

    <HowWeResearch />
    <FAQ {faqs} />
  </article>
</BaseLayout>
