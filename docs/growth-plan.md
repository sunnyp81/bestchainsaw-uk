# Growth Plan: bestchainsaw.uk

Written 2026-07-10, at Wave-1 build completion. This is a lean-scope portfolio tree (baseline ~£1.15/mo, realistic ceiling ~£3-7/mo within a year — see `docs/research-dossier.md` section 6) — the growth loop below is scaled to that, not to a flagship investment.

## 1. Wave 2/3 content schedule (conditional, not committed)

Wave 1 (11 URLs: homepage + 10 pages) is built. Per the lean-scope verdict, **Wave 2 is NOT scheduled upfront** — it is conditional on Wave-1 GSC impressions data existing. Trigger: once the site has 4+ weeks of live GSC data (see evolve-site enrolment below), review impressions per page and:

- If `/electric-chainsaws/`, `/cordless-chainsaws/`, or `/petrol-chainsaws/` show striking-distance queries (pos 10-30) that a disambiguation or comparison page would capture, build `/electric-vs-cordless-chainsaw/` first (top Wave-2 candidate per `docs/build-manifest.md`).
- If `/chainsaw-safety/` earns real impressions, split `/chainsaw-licence-uk/` out as its own page.
- If `/replace-bar/` earns impressions on sharpening-specific queries, split `/chainsaw-sharpening/` out.
- Full Wave-2/3 reference list (do not build speculatively): `/chainsaw-bar-length/`, `/chainsaw-kickback/`, `/chainsaw-noise-rules-uk/`, `/pole-saws/`, `/mini-chainsaws/`, `/chainsaw-brands-uk/`, `/chainsaw-chain-types/`, model-review spokes (Stihl MS 170, Titan TTL585CHN, Ryobi RY18CSA, EGO CS1400).
- Pace, if/when triggered: 1-2 pages/month, not the standard portfolio 2-3/week cadence — matches the site's small ceiling, avoid over-investing effort relative to return.

## 2. Monitor registration

- Add `bestchainsaw.uk` to `/seo-monitor-loop` triage once live (state will start as "unmonetised/launch" then move to "growth" once indexed).
- Register with seogets (`mcp__seogets__manage_site_tags` or equivalent) once a GSC property exists for the domain — needed before seogets can pull data.
- [MANUAL, blocked on deploy] Add GSC property `sc-domain:bestchainsaw.uk`, verify, submit sitemap (`https://bestchainsaw.uk/sitemap-index.xml`, already generated by `@astrojs/sitemap` in the build).
- [MANUAL, blocked on deploy] Add Bing Webmaster property + submit sitemap (Bing matters more than Google for new small sites per portfolio pattern — see thebestmowers' revenue-engine finding: Bing+DDG+Yahoo+AI >> Google in the first months of a new domain).

## 3. `/evolve-site` enrolment date

**Target: 2026-08-07** (launch + 4 weeks from today, adjust to actual deploy date once the Cloudflare blocker below is resolved — the clock should start from live-and-indexed, not from this build date). Do not enrol earlier; there won't be enough GSC data to iterate on.

## 4. Monetisation activation milestones

- Affiliate links: **already live in the code** (all `PickCard`/`PriceGrid` components use `amazonSearch()` tagged with `chainsaw0f6-21`) — activates automatically the moment the site is live, no separate step needed.
- Display ads: **do not add.** Per `/ad-rpm-optimizer` and the standing no-premature-AdSense rule, this site's realistic traffic ceiling (a few hundred sessions/month at best in year one) will never clear even the AdSense minimum (<10k sessions) let alone Ezoic's 10-50k gate. Revisit only if traffic wildly exceeds the dossier's projection.
- First-£ checkpoint: any single tracked order after relaunch (the site already has a working conversion loop — 2.70% historical CVR — so this is really a "first click after relaunch" checkpoint, not a cold-start).
- 90-day checkpoint figure to beat: **£3.46** (90 days at the £13.83/yr historical baseline) — if 90-day-post-relaunch earnings are meaningfully above that, the rebuild is working.

## 5. Entity corroboration checklist (carried over from Phase 1)

All flagged `[MANUAL]` in `site-identity.md` and the repo `CLAUDE.md`:
- Real `sameAs` profile URLs (LinkedIn, sunnypatel.co.uk author page, real Arbtalk/forum profile if genuine) — `SITE.sameAs` stays `[]` until supplied, never fabricate.
- Confirm publishing entity (personal vs ND Media LTD) — decides whether Companies House is a corroboration target.
- Real author image for Person schema (optional).
- Cross-link from sunnypatel.co.uk portfolio/author page once that page lists this site.

## 6. 90-day review

Scheduled for **90 days after live-and-indexed date** (not this build date). Compare actual traffic/earnings to the dossier's arithmetic (section 6): conservative ~£35-45/yr run-rate by month 6, moderate ~£70-90/yr by month 12. **If 90-day earnings are under 25% of the conservative line** (i.e. under ~£9/yr pace), trigger `/site-health` diagnosis rather than letting it decay silently — per the standing "plant many trees, grow every one" rule, a small tree still gets a real check-in, not neglect.

## Status note

This growth plan is written and the site is registration-ready, but **the site is not yet live** — deploy is blocked on a dead Cloudflare API token (see repo `CLAUDE.md` and the session handoff for detail). Dates above (evolve-site enrolment, 90-day review) are placeholders pending the actual go-live date and must be adjusted once deploy completes.
